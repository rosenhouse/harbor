expose:
  type: nodePort
  tls:
    enabled: false
externalURL: http://localhost:30002
# Persistent volumes keep registry blobs and database records in step when a pod restarts.
persistence:
  enabled: true
trivy:
  enabled: false
ipFamily:
  ipv6:
    enabled: false
