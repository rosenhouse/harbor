kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    # Harbor's NodePort, so e2e tests can seed it from the host.
    extraPortMappings:
      - containerPort: 30002
        hostPort: 30002
